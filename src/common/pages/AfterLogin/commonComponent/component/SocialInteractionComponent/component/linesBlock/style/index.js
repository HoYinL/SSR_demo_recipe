import { makeStyles } from "@material-ui/core/styles";

const LinesBlockStyle = makeStyles((theme) => ({
    root: {
        display: 'flex',
        width: '2.5rem',
        height: '100px',
    }
}))

export { LinesBlockStyle }
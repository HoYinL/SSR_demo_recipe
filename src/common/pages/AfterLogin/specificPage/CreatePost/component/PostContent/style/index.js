import { makeStyles } from "@material-ui/core/styles";

const CreatePostStyle = makeStyles((theme) => ({
    root: {
        padding: '1rem 0',
        width: "100%",
        textAlign: 'center',
    }
}))

export { CreatePostStyle }